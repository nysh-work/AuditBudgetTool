// src/components/ui/tabs/TabsList.jsx
import React from 'react';

const TabsList = ({ children }) => {
  return (
    <div className="tabs-list">
      {children}
    </div>
  );
};

export default TabsList;